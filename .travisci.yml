language: node_js

node_js:
   - "7"
   - "8"

branches:
  only:
    - master

before_script:
   - npm install -g @angular/cli

script:
   - ng build --prod

cache:
  directories:
    - "node_modules"
